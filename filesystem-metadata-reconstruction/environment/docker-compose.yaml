version: '3.8'

services:
  benchmark:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: filesystem-metadata-reconstruction
    working_dir: /workspace
    user: benchmark
    volumes:
      - ./solution:/oracle:ro
      - ./tests:/tests:ro
    command: ["/bin/bash"]
